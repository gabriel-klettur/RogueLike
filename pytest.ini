[pytest]
minversion = 6.0
addopts = --maxfail=1 --import-mode=importlib
testpaths = tests
python_files = test_*.py
markers =
    slow: marks tests as slow; deselect with '-m "not slow"'
filterwarnings =
    ignore:pkg_resources is deprecated as an API.*:UserWarning:pygame\\.pkgdata
