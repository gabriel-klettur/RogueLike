from __future__ import annotations

from typing import Any, Dict


def load_layout(data: Dict[str, Any]) -> None:  # placeholder signature
    """Load layout data into the model (stub)."""
    return None


def save_layout(model: Any) -> Dict[str, Any]:
    """Serialize layout data from the model (stub)."""
    return {}
