"""Constants used by the Entities State Tabs UI.

Keep UI labels centralized here so controllers, models and views stay in sync.
"""

# Default list of state tabs shown in the Properties panel.
# Note: keep order stable for predictable rendering.
STATE_TABS_DEFAULT = [
    "idle",
    "walk",
    "chase",
    "attack",
    "death",
    "damage",
    "casting",
]
