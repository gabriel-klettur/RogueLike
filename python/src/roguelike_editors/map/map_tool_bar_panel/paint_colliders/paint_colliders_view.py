class PaintCollidersView:
    """
    Minimal view stub for the Paint Colliders tool.
    The confirmation dialog is rendered by MapEditorView, so this is a placeholder
    for potential future overlays.

    Español: Vista mínima; los diálogos se dibujan en `MapEditorView`.
    """

    def __init__(self, controller, model):
        self.controller = controller
        self.model = model

    def render(self, screen):
        # No-op por ahora
        return None
