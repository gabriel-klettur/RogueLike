from __future__ import annotations

from typing import Any, Tuple


def compute_graph_bounds(model: Any) -> Tuple[float, float, float, float]:
    """Compute (min_x, min_y, max_x, max_y) of all nodes (stub)."""
    return (0.0, 0.0, 0.0, 0.0)
